import java.util.Scanner;
public class RosterManager {
    Scanner sc  = new Scanner(System.in);
    public void run() { // the next line will not work because what if the only command is "P"??
        System.out.println("Roster Manager running...");
        System.out.println();
        /*
        String inp = sc.next();
        if (inp.equals("A")) {
            String f = sc.next();
            String l = sc.next();
            Date d = new Date(sc.next());
            Profile p = new Profile(f, l, d);
            Major m = ;// IDK HOW TO DO THIS YET
            Student s = new Student(p, m, sc.nextInt());
        } else if (inp.equals("R")) {
            String f = sc.next();
            String l = sc.next();
            Date d = new Date(sc.next());
        } else if (inp.equals("P")) {
            // display roster sorted by last name, first name, and DOB
            // does this mean that we have to print in order of each? PROBABLY
        } else if (inp.equals("PS")) {
            // display roster sorted by standing
        } else if (inp.equals("PC")) {
            // display the roster sorted by school and major
        } else if (inp.equals("L")) {
            // list students in a specified school, sorted by ast name, first name, and DOB.
            String school = sc.next();
            // unfinished method
        } else if (inp.equals("C")) {
            String f = sc.next();
            String l = sc.next();
            Date d = new Date(sc.next());
            Major m = ; // STILL NEED TO KNOW HOW TO DO THIS
            // UNFINISHED METHOD: change a student's major
        } else if (inp.equals("Q")) {
            // stop the program execution
            System.out.println("Roster Manager terminated");
        } else {
            // DOESNT WORK DO WE THROW EXCEPTION OR SOMETHING?
        }
         */
    }
}
